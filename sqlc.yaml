version: "2"
sql:
  - engine: "mysql"
    queries: "db/queries/"
    schema: "db/schema.sql"
    gen:
      go:
        package: "db"
        out: "db/sqlc"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false